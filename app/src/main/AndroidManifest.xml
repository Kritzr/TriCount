<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TriCount">
        <activity android:name=".TricountDetailActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".TemporaryActivity" />
        <activity android:name=".AddTricountActivity" />
        <activity android:name=".JoinTricountActivity" />
        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- Sign Up Activity -->
        <activity
            android:name=".SignUpActivity"
            android:exported="false" />

        <!-- Home Activity -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <!-- Add Tricount Activity -->
        <activity
            android:name=".AddTricountActivity"
            android:exported="false" />

        <!-- Join Tricount Activity -->
        <activity
            android:name=".JoinTricountActivity"
            android:exported="false" />

        <!-- Tricount Detail Activity (if you have this) -->
        <activity
            android:name=".TricountDetailActivity"
            android:exported="false" />
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>


    </manifest>